{
  "name": "tradediary",
  "version": "0.1.0",
  "description": "Open source trading journal — track, analyze, and improve your trading performance",
  "private": true,
  "workspaces": [
    "packages/server",
    "packages/client"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w packages/server\" \"npm run dev -w packages/client\"",
    "build": "npm run build -w packages/server && npm run build -w packages/client",
    "test": "npm run test -w packages/server && npm run test -w packages/client",
    "lint": "npm run lint -w packages/server && npm run lint -w packages/client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
